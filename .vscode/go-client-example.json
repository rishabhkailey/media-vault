{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "[Debug] Launch Go Client Example",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/examples/go-client/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}